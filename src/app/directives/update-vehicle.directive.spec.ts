import { UpdateVehicleDirective } from './update-vehicle.directive';

describe('UpdateVehicleDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateVehicleDirective();
    expect(directive).toBeTruthy();
  });
});
