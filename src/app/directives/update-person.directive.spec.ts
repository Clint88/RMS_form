import { UpdatePersonDirective } from './update-person.directive';

describe('UpdatePersonDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdatePersonDirective();
    expect(directive).toBeTruthy();
  });
});
