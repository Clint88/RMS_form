import { UpdateIncidentDirective } from './update-incident.directive';

describe('UpdateIncidentDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateIncidentDirective();
    expect(directive).toBeTruthy();
  });
});
